"use strict";

for (let b = 0; b < 3; b++) {
  console.log(`------- starting the exercise${b}`);

  for (let c = 0; c < 6; c++) {
    console.log(`The first exercise is ${c} reps`);
  }
}
let c = 1;
while (c <= 10) {
  console.log(`The first exercise is ${c} reps`);
  c++;
}
