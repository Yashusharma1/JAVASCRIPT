'use strict';
// FIRST LECTURE

// dom is the object which is used to select the element

// lets say we want to select the elelemt from the html then we will use the query selector to select the element

console.log(document.querySelector('.message'));
// now if want to  show the message the we will use the textcontent
console.log(document.querySelector('.message').textContent);
